# Sportif lifestyle + product calm blend (9:16)

The editorial "world -> product" story: SPORTIF -> lifestyle "For your morning ritual." ->
flatlay "Three strengths." -> lifestyle "Everyday training, elevated." -> band "Find your
resistance." -> "Coming soon". Framed card-on-peach (rounded + soft shadow), gentle Ken Burns,
one caption per beat. Lifestyle prints and product cards get the same framed treatment so it
reads as one considered lookbook.

Fixed index.html (not generator-driven) — 6 scenes, opacity:0 + `.to()` reveals (lint-clean).
`.p45` = 4:5 cards, `.land` = the landscape flatlay card. `_v1_MUSICDEMO.mp4` carries a calm
100 BPM scratch bed (preview only, not licensed). Works by juxtaposition (see the pipeline doc
`clients/sportif/products/real-bands-content-process.md`). Renders/audio/snapshots gitignored.
